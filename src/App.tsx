// Routes
import { AppRoutes } from '@/routes';

function App() {
     return (
          // No futuro, aqui entrarão outros Providers (Auth, Theme, etc.)
          <AppRoutes />
     );
}

export default App;